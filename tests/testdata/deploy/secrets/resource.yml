apiVersion: shrine/v1
kind: Resource
metadata:
  name: secret-store
  owner: shrine-deploy-test
spec:
  type: cache
  version: "1"
  port: 6379
  image: traefik/whoami
  env:
    - name: password
      generated: true
  outputs:
    - name: host
    - name: port
    - name: password
    - name: connection
      template: "redis://{{.host}}:{{.port}}"
  networking:
    exposeToPlatform: false
