apiVersion: shrine/v1
kind: Resource
metadata:
  name: hello-db
  owner: team-a
  access:
    - team-b
spec:
  type: postgres
  version: "16"
  port: 5432
  env:
    - name: database
      value: "hello"
    - name: password
      generated: true
  outputs:
    - name: host
    - name: port
    - name: database
    - name: url
      template: "postgres://postgres:{{.password}}@{{.host}}:{{.port}}/{{.database}}"
  networking:
    exposeToplatform: false
